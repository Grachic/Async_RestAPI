<Window x:Class="Async_RestAPI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Async_RestAPI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*" />
            <RowDefinition Height="0.2*" />
            <RowDefinition Height="0.2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Content="Async Project" FontSize="16" Margin="10"/>

        <Button Click="ButtonSync_Click" Content="Load Sync" Grid.Row="1" FontSize="20" Margin="10" />
        <Button Click="ButtonAsync_Click" Content="Load Async" Grid.Row="2" FontSize="20" Margin="10" />

        <TextBlock x:Name="textBlockInfo" Grid.Row="3" Text="Info" FontSize="20" Margin="10" />
    </Grid>
</Window>
